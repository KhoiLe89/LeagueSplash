<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    {{#each unique }}
    <h3>  {{this.name}}</h3>
        <img src="{{this.skinImg}}">
      {{/each}}
            {{#each skinData}}


              {{#each this.skins}}

            <img src="{{this.skinImg}}">

              {{/each}}
            {{/each}}

    {{#each skinData}}

    <h3> {{this.name}} </h3>
      {{#each this.skins}}

      <h3> {{this.skinImg}}</h3>

      {{/each}}
    {{/each}}
{{skinInfo}}

  </body>
</html>
